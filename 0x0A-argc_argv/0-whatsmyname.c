#include <stdio.h>
#include "main.h"
/**
 * main - the function
 * @argv: the parameter
 * @argc: the parameter
 * Return: 0
 */
int main(int argc _attribute_((unused)), char *argv[])
{
	_putchar(*argv);
	_putchar('\n');

	return (0);
}
